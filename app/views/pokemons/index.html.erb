<h1>Pokemons</h1>

<ul>
  <% @pokemons.each do |pokemon| %>
    <li>
      <%= link_to pokemon.name, pokemon %>
    </li>
  <% end %>
</ul>

<%== pagy_nav(@pagy) %>

<ul>
  <li><%= link_to "Create a new Pokemon", new_pokemon_path(@pokemon) %></li>
</ul>
